// scripts/users/create.ts — pnpm user:create --email X --name Y --role editor
// TODO(Phase 1): full implementation per TZ 2.7.2 — interactive password prompt,
// --temp-password and --send-email options, validation, audit log to logs/user-management.log.
console.error('Not implemented yet. Will be wired up in Phase 1.')
process.exit(2)
